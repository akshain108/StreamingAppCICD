apiVersion: v1
kind: Service
metadata:
  name: auth-svc
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP

  selector:
    app: auth

  ports:
    - name: http
      port: 3001
      targetPort: 3001