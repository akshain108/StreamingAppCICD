apiVersion: v1
kind: Service
metadata:
  name: admin-svc
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP

  selector:
    app: admin

  ports:
    - name: http
      port: 3003
      targetPort: 3003