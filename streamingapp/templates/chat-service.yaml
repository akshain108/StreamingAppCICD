apiVersion: v1
kind: Service
metadata:
  name: chat-svc
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP

  selector:
    app: chat

  ports:
    - name: http
      port: 3004
      targetPort: 3004